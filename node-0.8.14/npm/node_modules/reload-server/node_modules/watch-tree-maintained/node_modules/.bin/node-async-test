#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../async_testing/bin/node-async-test.js" "$@"
  ret=$?
else 
  node  "$basedir/../async_testing/bin/node-async-test.js" "$@"
  ret=$?
fi
exit $ret
